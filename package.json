{
  "name": "discord-loyalty-bot",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that rewards Discord members with LTZ tokens for community participation.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "author": "Loyalteez",
  "license": "MIT",
  "dependencies": {
    "discord-interactions": "^3.4.0",
    "discord.js": "^14.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

